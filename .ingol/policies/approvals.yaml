schema_version: "1"
profile: bootstrap-controlled
controlled:
  owner_approval: required
  independent_review: required
  checker_workflow_review: required
standard:
  independent_review: required
fast:
  independent_review: optional
